import { GetDischargeVerificationData } from './get-discharge-verification-data.model';

describe('GetDischargeVerificationData', () => {
  it('should create an instance', () => {
    expect(new GetDischargeVerificationData()).toBeTruthy();
  });
});
