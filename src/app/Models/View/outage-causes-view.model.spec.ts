import { OutageCausesView } from './outage-causes-view.model';

describe('OutageCausesView', () => {
  it('should create an instance', () => {
    expect(new OutageCausesView()).toBeTruthy();
  });
});
