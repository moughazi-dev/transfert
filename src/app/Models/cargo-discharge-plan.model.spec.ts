import { CargoDischargePlan } from './cargo-discharge-plan.model';

describe('CargoDischargePlan', () => {
  it('should create an instance', () => {
    expect(new CargoDischargePlan()).toBeTruthy();
  });
});
