import { CreateDischargePlan } from './create-discharge-plan.model';

describe('CreateDischargePlan', () => {
  it('should create an instance', () => {
    expect(new CreateDischargePlan()).toBeTruthy();
  });
});
