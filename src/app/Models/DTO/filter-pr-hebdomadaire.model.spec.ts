import { FilterPrHebdomadaire } from './filter-pr-hebdomadaire.model';

describe('FilterPrHebdomadaire', () => {
  it('should create an instance', () => {
    expect(new FilterPrHebdomadaire()).toBeTruthy();
  });
});
