import { CargoDischargeMode } from './cargo-discharge-mode.model';

describe('CargoDischargeMode', () => {
  it('should create an instance', () => {
    expect(new CargoDischargeMode()).toBeTruthy();
  });
});
