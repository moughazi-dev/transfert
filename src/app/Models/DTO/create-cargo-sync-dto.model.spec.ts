import { CreateCargoSyncDTO } from './create-cargo-sync-dto.model';

describe('CreateCargoSyncDTO', () => {
  it('should create an instance', () => {
    expect(new CreateCargoSyncDTO()).toBeTruthy();
  });
});
