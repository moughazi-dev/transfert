import { DischargeVerificationDataDTO } from './discharge-verification-data-dto.model';

describe('DischargeVerificationDataDTO', () => {
  it('should create an instance', () => {
    expect(new DischargeVerificationDataDTO()).toBeTruthy();
  });
});
