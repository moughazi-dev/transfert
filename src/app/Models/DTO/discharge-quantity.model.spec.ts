import { DischargeQuantity } from './discharge-quantity.model';

describe('DischargeQuantity', () => {
  it('should create an instance', () => {
    expect(new DischargeQuantity()).toBeTruthy();
  });
});
