import { StartCargoDischarge } from './start-cargo-discharge.model';

describe('StartCargoDischarge', () => {
  it('should create an instance', () => {
    expect(new StartCargoDischarge()).toBeTruthy();
  });
});
