import { CreateCargoDischarge } from './create-cargo-discharge.model';

describe('CreateCargoDischarge', () => {
  it('should create an instance', () => {
    expect(new CreateCargoDischarge()).toBeTruthy();
  });
});
