import { CargoDischargeType } from './cargo-discharge-type.model';

describe('CargoDischargeType', () => {
  it('should create an instance', () => {
    expect(new CargoDischargeType()).toBeTruthy();
  });
});
